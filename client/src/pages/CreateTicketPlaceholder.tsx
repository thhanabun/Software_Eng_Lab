export default function CreateTicketPlaceholder() {
  return (
    <div className="tg-card">
      <h1 className="h4 mb-2">Create Ticket</h1>
      <p style={{ color: 'var(--tg-muted)' }}>
        Ticket creation form with validation and attachment selection — implemented in Issue #14.
      </p>
    </div>
  )
}
